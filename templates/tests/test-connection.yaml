apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "REPLACE.fullname" . }}-test-connection"
  labels:
    {{- include "REPLACE.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "REPLACE.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
