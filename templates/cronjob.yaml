apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: backup-REPLACE
  annotations: {}
spec:
  schedule: "0 0 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
        {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 13 }}
        {{- end }}
          serviceAccountName: {{ include "REPLACE.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 13 }}
          volumes:
            - name: config-volume
              configMap:
                name: REPLACE-configmap
            - name: wp-uploads-{{ .Chart.Name }}
              persistentVolumeClaim:
                claimName: wp-uploads-{{ .Chart.Name }}
          containers:
            - name: {{ .Chart.Name }}-dailybackup
              env:
                - name: DATABASE_HOST
                  value: "{{ .Chart.Name }}-mysql.wordpress.svc"
                - name: DATABASE_USER
                  valueFrom:
                    secretKeyRef: 
                      name: REPLACE-db-creds-secret
                      key: user
                - name: DATABASE_NAME
                  valueFrom:
                    secretKeyRef:
                      name: REPLACE-db-creds-secret
                      key: name
                - name: DATABASE_PASS
                  valueFrom:
                    secretKeyRef:
                      name: REPLACE-db-creds-secret
                      key: pass
                - name: BUCKET
                  value: "hostkraken-backup"
                - name: AWS_ACCESS_KEY_ID
                  valueFrom: 
                    secretKeyRef:
                      name: do-backup-secret
                      key: access_key
                - name: AWS_SECRET_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: do-backup-secret
                      key: secret_key
                - name: SITE_NAME
                  value: DOMAIN
              volumeMounts:
              - name: config-volume
                mountPath: /configs
              - name: wp-uploads-{{ .Chart.Name }}
                mountPath: /usr/html/wp-content/uploads
              securityContext:
                {{- toYaml .Values.securityContext | nindent 17 }}
              image: "registry.hostkraken.com/dailybackup:latest"
              imagePullPolicy: Always
              args:
              - /bin/backup-site REPLACE
