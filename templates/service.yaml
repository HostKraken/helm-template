apiVersion: v1
kind: Service
metadata:
  name: {{ include "REPLACE.fullname" . }}
  labels:
    {{- include "REPLACE.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "REPLACE.selectorLabels" . | nindent 4 }}
