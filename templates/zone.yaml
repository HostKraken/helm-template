apiVersion: crds.kubeflare.io/v1alpha1
kind: Zone
metadata:
  name: {{ .Values.domain }}
  namespace: kubeflare-system
spec:
  apiToken: kubeflare-token
  settings:
    alwaysUseHttps: true
    alwaysOnline: true
    minify:
      css: true